selenium
test fonctionnel de page web automatisé
different de test de code => test unitaire

langage type => int a = 1;
bytecode dans une jvm : portabilité multios
compiltation pour avoir le bytecode et qui est executé dans la jvm : langage interpreté
classe extension .java
fichier compiler .class
c est le fichier .class qui s execute sur les serveurs
public void ne retourne pas d information
selenium toujouirs des jar (pas de war)

Dans eclipse 
- dev travaille  dans src/main
- selenium travaille dans /src/test
les repertoires resources sont les fichiers de config
CRTL Shift F : indentation
// commentaire
obliger import de classe si on n est pas dans la meme arboresence
this.variable => affection de l instance et pas local
 ex this.color = color Vs  color = colora
pour endre les attributs visible les argument => les mettre en public
 Ex public String marque = "BMW"

public void => methode
 ||= OR
 && =AND
 
Tableau
Tab[0]=1 lecture
affection 
tab[2]=24


https://mvnrepository.com/
pom.xml 
 * maven => version jdk
 * dependance selenium
 

 